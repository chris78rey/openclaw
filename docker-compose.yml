services:
  cli:
    image: ${CLI_IMAGE:-node:22-bookworm}
    container_name: bot-cli
    init: true
    stdin_open: true
    tty: true
    command: ["bash"]
    networks:
      - coolify

networks:
  coolify:
    external: true
