services:
  cli:
    image: ${CLI_IMAGE:-alpine:3.20}
    container_name: bot-cli
    init: true
    stdin_open: true
    tty: true
    command: ["sh"]
    networks:
      - coolify

networks:
  coolify:
    external: true
